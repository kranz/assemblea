<div class="jumbotron">
  <% if user_signed_in? %>
    <h1>Bentornato, caro il mio utente!</h1>
    <p class="lead">
      You have signed in!  If this were a real app some sweet conent
      would go here.  But it's just a test.  You might want to sign out now.
    </p>
    <%= link_to "Logut", destroy_user_session_path, :method => :delete, :class => 'btn btn-large btn-danger' %>
  <% else %>
    <h1>Benvenuto, o nuovo utente!</h1>
    <p class="lead">
      You must sign in or sign up before you can access our super sweet,
      top secret content.
    </p>
    <div class="row-fluid">
      <div class="span6">
        <%= link_to "Login", new_user_session_path, :class => "btn btn-success" %>
      </div>
      <div class="span6">
        <%= link_to "Registrazione", new_user_registration_path, :class => "btn btn-success" %>
      </div>
    </div>
  <% end %>
</div>